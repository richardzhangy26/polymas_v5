# Polymas V5 专家与 Skill 开发规则

## 语言

- 开发交流、计划、文档、测试说明和交付总结使用中文。
- 平台要求固定英文的字段、命令、Skill 名和状态值保持原文。

## 项目目标

本目录用于研究、创建、验证和部署 Polymas V5 专家智能体与 Skill。实现时保持以下分层：

1. `AGENTS.md`：约束开发本项目的 Agent，不上传到平台。
2. `Agent.md`：定义平台专家的身份、职责、工作流、红线和技能路由。
3. `SKILL.md`：定义单项专业能力的触发条件、输入输出和执行边界。
4. `scripts/`：承载需要确定性、可重复执行的数据处理或接口调用。
5. `references/` 与 `output_format/`：分别保存按需读取的契约和用户可见格式。

专家负责理解意图与编排；Skill 负责专业能力；内置工具负责问答、文件、时间、Cron 和消息等基础动作。提示词不能代替不存在的 API 或平台权限。

## 资料入口

| 任务 | 入口 |
|---|---|
| 工程师制作专家智能体 | `.agents/skills/building-polymas-experts/` |
| Polymas Skill 结构与调用规范 | `Polymas Skill V5参考手册.md` |
| 专家与 Skill 规划、NID、测试状态 | `技能  - 专家整理.xlsx` |
| 平台能力、生产映射、Cron 等设计信息 | `技能一览表.xlsx` |
| 能力边界与历史调研 | `docs/research/` |
| 已有定时资讯模式 | `daily-tech-news/` |
| 学生画像与个性化推送参考 | `polymas-student-profile-expert-skills/` |
| 教师日历 Skill 参考 | `polymas-teacher-work-calendar/` |

表格里的设计条目、待提测条目和历史快照不等同于当前线上能力。线上绑定、真实联调和发送仍须核对当前 PDS/SkillHub。工程师用 `building-polymas-experts` 做选型与 Agent.md 草案时，以两份 xlsx 和 `docs/atomic-skill-catalog.json` 为权威源，不把 PDS/SkillHub 在线核验当作该流程的交付阻断。

## 专家创建流程

1. **盘点现状**：读取目标专家的基本信息、Agent.md 和已挂载技能。
2. **能力复用**：优先使用平台已存在且角色权限匹配的 Skill；避免挂载多个职责重复的检索或写入 Skill。
3. **识别缺口**：只有平台现有能力无法覆盖的领域逻辑才创建新 Skill。
4. **编排 Agent.md**：明确任务接收、工具选择、执行顺序、失败状态、维护入口和完成条件。
5. **真实交互**：缺少用户选择时调用 `ask_user_question` 并等待，不用普通文本假装暂停。
6. **写入确认**：发布、发送、删除、订阅和权限变化遵守平台确认规则；浏览器最终保存前再次确认。
7. **联调验证**：分别验证生成成功、平台写入成功、发送成功、接收成功和已读状态，不混为一个“成功”。

## 平台 Agent.md 交付模板

交付专家配置时，使用 PDS 提供的 Agent.md 模板结构，不改动变量名；在对应章节中补充领域职责、固定流程和安全约束。实际文件中的变量下划线不转义，例如写 `${agent_name}`，而不是 `${agent\_name}`。

```markdown
---
name: ${agent_name}
---

您是${agent_name}。

${expertise}

## 您的角色

- 分析需求并提供专业支持
- 运用绑定的技能完成任务
- 遵循工作流程规范执行

## 核心职责

${core_responsibilities}

## 可用技能

${skills}

## 工作流程

### 1. 任务接收

- 理解用户需求
- 确定任务类型和复杂度
- 选择合适的技能和工具

### 2. 任务执行

- 按照流程规范操作
- 调用相关工具完成任务
- 实时反馈进度

### 3. 结果交付

- 结构化呈现结果
- 确保输出符合格式要求
- 提供必要的解释说明

### 动态流程

${workflow}

## 我不做什么

${boundaries}

## 工作风格

${work_style}

## 最佳实践

1. **结论先行**：先给出结果，再展开细节
2. **主动沟通**：发现问题及时反馈
3. **持续改进**：根据反馈优化工作方式
```

交付时同时提供：

1. 可直接粘贴到 PDS 的完整 Agent.md；
2. `${expertise}`、`${core_responsibilities}`、`${workflow}`、`${boundaries}`、`${work_style}` 的字段内容；
3. 精确技能名称、版本和挂载顺序；
4. 专家开场白、推荐问题和真实联调清单。

## Skill 目录与契约

Polymas Skill 默认采用：

```text
skill-name/
├── SKILL.md
├── references/
├── output_format/
└── scripts/
    └── base/
```

- Skill 文件夹名与 frontmatter `name` 一致，使用小写字母、数字和连字符。
- `description` 描述触发场景和边界，避免吸引无关请求。
- `SKILL.md` 至少包含：技能说明、触发/不触发条件、项目结构、执行流程、暂停确认规则、执行流程强制约束。
- 固定流程使用 `[DETERMINE]`、`[CALL]`、`[FILTER]`、`[BUILD]`、`[CONFIRM]` 标记。
- 复杂数据结构、来源策略和接口字段写入 `references/`，不要在多个文件复制同一契约。
- 用户可见输出模板写入 `output_format/`。
- 脚本成功时 stdout 输出单个 JSON；失败时 stdout 输出 `{"error":"可转述错误"}`，调试信息写 stderr。
- 不在 Skill、测试、日志或 ZIP 中保存 Token、Cookie、Authorization、真实学生 ID 或其他凭证。

## 设计原则

- **DRY**：画像、订阅、任务、来源和输出字段各自只有一个权威契约。
- **SOLID**：检索、清洗、点评、调度和投递职责分离；领域 Skill 不越权管理 Cron 或消息通道。
- **KISS**：优先完成一个可测试的闭环；实时行情、复杂推荐和多专家协作仅在需求明确后增加。
- **证据优先**：事实、来源、课程证据和模型分析分别存储与展示。
- **失败可见**：无结果、权限不足、目标不唯一、部分成功和重试失败必须返回真实状态。

## 定时任务与投递

- Cron 任务必须使用稳定幂等键，并显式传入当前 `--agent-id`。
- 创建任务前查询现有任务；修改计划采用可回滚切换，避免双任务并行。
- 定时执行前和消息发送前都重新校验订阅状态、任务版本、身份和目标会话。
- 个人内容只发送到唯一匹配的个人会话；目标不唯一时停止，不降级到班级群。
- 消息工具确认成功后才能写入成功历史和更新时间。
- 订阅必须支持查询、修改、暂停、恢复和退订。

## 财经新闻课程点评边界

- 专家不预先绑定课程；学生首次使用时从本人可访问课程中选择一门。
- 一份订阅绑定一名学生、一门课程和一个个人会话。
- 财经新闻必须保留来源、URL、发布时间和检索时间。
- 新闻事实、课程依据、理论分析和讨论问题分栏呈现。
- 未检索到课程证据时不生成课程点评，也不以通用常识冒充课程理论。
- 不提供买入、卖出、目标价、收益承诺、投资组合或交易操作。
- 默认不依赖第三方 API Key、登录 Cookie、付费新闻账户或实时行情接口。
- 不绕过验证码、登录墙、反爬安全页或付费墙。

## 测试与验证

- 新功能和脚本遵循测试先行：先看到目标测试因能力缺失而失败，再实现最小代码。
- 新 Skill 先做无 Skill 行为基线，再做独立 forward-test。
- 定向测试优先放在对应项目的 `tests/` 下，避免依赖无关课程素材。
- Skill 基础校验：

```bash
python /Users/zhangyichi/.codex/skills/.system/skill-creator/scripts/quick_validate.py <skill_dir>
```

- Python 定向测试：

```bash
python -m pytest -q <project>/tests
```

- 交付前执行 `git diff --check`，检查 ZIP 内容，确认不含隐藏文件、缓存、测试、凭证和本地路径。
- 仓库全量测试若因主工作区未跟踪的课程素材缺失而失败，记录基线并运行本项目独立测试；不得把既有失败归因于当前修改。

## Git 与文件安全

- 功能开发使用 `codex/` 前缀的隔离分支或 worktree，不直接在 `main` 上实现。
- 当前工作树中的未跟踪文件属于用户，不移动、不覆盖、不清理。
- 使用 `apply_patch` 进行文本编辑；批量格式化仅限明确目标。
- 不执行 `git reset --hard`、`git clean`、强制推送或其他不可恢复操作。
- 未经用户授权不上传 Skill、不绑定线上专家、不保存线上配置、不发送测试消息。

## 完成标准

只有同时满足以下条件才能报告完成：

1. 项目定向测试通过；
2. Skill 校验通过；
3. ZIP 可解压且目录结构正确；
4. Agent.md 与 Skill 的职责边界一致；
5. 无凭证、真实身份和敏感信息；
6. 独立审查无未解决的严重或重要问题；
7. 线上能力仅报告已经真实联调验证的状态。
